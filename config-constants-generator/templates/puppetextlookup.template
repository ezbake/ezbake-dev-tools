class ezconfig::params {
{% for group, consts in constants.iteritems() %}

	# [{{group}}]
{% for const in consts %}
	# (type: {{const.type_name}}) {{const.description}}
	${{const.variable_name}}			= extlookup("{{const.property_name}}", undef, 'ezconfig')
{% endfor %}
{% endfor %}
}
